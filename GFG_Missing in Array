class Solution {
    int missingNum(int arr[]) {
        int n=arr.length;int num=1;
        Arrays.sort(arr);
        if(n==1) num=arr[0]+1;
        for(int i=0;i<n;i++){
                if(arr[i]!=i+1){
                    return i+1;
            }
        }
        return n+1;
    }
}
