class Solution {
    public Pair<Integer, Integer> getMinMax(int[] arr) {
        int n=arr.length;
        Arrays.sort(arr);
        int min=arr[0];
        int max=arr[n-1];
        return new Pair(min,max);
    }
}
