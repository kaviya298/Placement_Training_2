class Solution {
    public int reverseExponentiation(int n) {
        int k=n;int rev=0;
        while(n!=0){
            int digit=n%10;
            rev=rev*10+digit;
            n=n/10;
        }
        int result=(int)Math.pow(k,rev);
        return result;
    }
}
